# UI-Style-Contract

Entwicklerleitlinie für Layout, Design und UI. **SSoT: Startseite** (`/`, `Start.tsx` + `start.css`).

---

## 1. Startseite (NICHT ÄNDERN)

- Startseite ist Single Source of Truth.
- Keine Änderungen an `src/screens/Start.tsx`, `src/screens/start.css`, layout.css (Startseiten-relevante Variablen `--hero-shift`, `--baby-shift`, `--footer-height`).

---

*Warten auf weitere Anweisung.*
